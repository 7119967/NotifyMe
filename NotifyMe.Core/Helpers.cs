namespace NotifyMe.Core;

public class Helpers
{
    // public int GetNewIdEntity<TEntity>(ICollection<TEntity> sequence)
    // {
    //     return sequence.Any() ? sequence.Max(e => Convert.ToInt32(e.Id)) + 1 : 1;
    // }
}